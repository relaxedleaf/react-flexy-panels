import { variant as defaultVariant } from "./default";
import { variant as handleVariant } from "./handle";
import { variant as verticalVariant } from "./vertical";

export const variants = {
  Default: defaultVariant,
  Handle: handleVariant,
  Vertical: verticalVariant,
};
